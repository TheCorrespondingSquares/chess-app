<div class="row text-center">
  <div class="columns container">
    <h1><%= @game.name %></h1>
    <br />
    <table class="center">
      <% 8.times do |row| %>
        <tr>
        <% 8.times do |col| %>
          <% if (row + col) % 2 == 0 %>
            <td class="black_tile">
              <% row %>  
                <% piece = @pieces.where(x_pos: col, y_pos: row).first %>
                <% if piece.blank? == false %>
                  <%= link_to piece.name, game_piece_path(@game, piece) %>
                <% end %>
              <% col %>
            </td>
          <% else %>
            <td class="white_tile">
              <% row %>
                <% piece = @pieces.where(x_pos: col, y_pos: row).first %>
                <% if piece.blank? == false %>
                  <%= link_to piece.name, game_piece_path(@game, piece) %>
                <% end %>
              <% col %>
            </td>
          <% end %>
        <% end %>
        </tr>
      <% end %>
    </table>
  </div>
</div>
    