    <div class="modal fade" id="pawnPromote" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">×</span>
            </button>
            <h4 class="modal-title" id="myModalLabel"> Select Your New Piece!</h4>
          </div>
          <%= simple_form_for @game do |f| %>
          <div class="modal-body">
            	<%= f.button :submit, 'Rook', name: 'name', value: 'Rook' %>
              <%= f.button :submit, 'Bishop', name: 'name', value: 'Bishop' %>
              <%= f.button :submit, 'Knight', name: 'name', value: 'Knight' %>
              <%= f.button :submit, 'Queen', name: 'name', value: 'Queen' %>
              <%= f.button :submit, 'Pawn', name: 'name', value: 'Pawn' %>
            </ div>
          <% end %>
        </div>
      </div>
    </div>
